{{ component('hike_form',  {
    hike: hike.id ? hike: null,
    form: form
})}}


{# 	{{ form_start(form) }}
	{{ form_widget(form) }}
	<div class="d-flex justify-content-between">
		<a class="btn btn-tertiary" href="{{ path('app_admin_hike_index') }}">Retour</a>
		<button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
	</div>
	{{ form_end(form) }} #}
